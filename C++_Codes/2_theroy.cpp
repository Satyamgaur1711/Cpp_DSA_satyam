#include <iostream>
using namespace std;



int main(){
    cout << "satyam gaur Er. \n";

    int x = 45;
    printf("%d \n", x);
    cout << "the size of int variable in bytes  is  \n";
    cout  << sizeof(x);

    // deta types in c++ int float dubble char bool
    // int take 4 bytes 
    // char take 1 bytes
    // 8 bits = 1 bytes. 
    // int me 32 bits ke space reserve ho jati hai.taki waha py koi bhi intege aa paye.
    // ascii value of each alphabate are avalable. when we store a charector that realy store ascii value of the that alphabate.

    return 0;
}